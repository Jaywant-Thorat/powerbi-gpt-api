services:
  - type: web
    name: powerbi-gpt-api
    env: python
    buildCommand: |
      apt-get update && apt-get install -y unixodbc-dev
      pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT